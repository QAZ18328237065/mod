languages = {
	check = "simp_chinese"
	check = "english"
}
filter = {
	show_vanilla = no
	show_loaded_mods = no
	# 误报: random = {...} else = {...} 是原版标准用法(全游戏 4268 处 else),
	# Tiger 对 random 内嵌 if 的 else 配对检测有误报,精确排除本文件
	trigger = {
		NOT = {
			AND = {
				key = if-else
				file = common/scripted_effects/zzz_universal_investment_effects.txt
			}
		}
	}
}
